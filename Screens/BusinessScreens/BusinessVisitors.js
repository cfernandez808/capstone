import React, { useState, useEffect } from "react";
import { Button, View, TextInput } from "react-native"

const BusinessVisitors = () => {
  return (<View
    style={{
      flex: 1,
      alignItems: "center",
      justifyContent: "center",
    }}>
      <Button title="Go back to profile" />
    </View>
  );
}

export default BusinessVisitors;
